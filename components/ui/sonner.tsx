'use client'

// Lightweight toast placeholder. The app references <Toaster /> in the layout.
// For acceso libre we keep it minimal — no external sonner dependency needed.
export function Toaster() {
  return null
}
